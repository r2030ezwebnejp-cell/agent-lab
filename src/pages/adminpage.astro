---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
const withBase = (p: string) => `${base.endsWith('/') ? base : base + '/'}${p}`;
---
<BaseLayout title="管理者ページ" description="管理者ページ">
  <section class="admin-toggle">
    <h1>管理者ページ</h1>
    <p>このページを表示している間だけ、ヘッダーの「お問い合わせ」の左に「設定」リンクが表示されます。他のページに移動すると「設定」は再び非表示になります。</p>
    <a class="back" href={withBase('settings/')}>設定を開く</a>
  </section>
</BaseLayout>

<style>
  .admin-toggle {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 32px 28px;
    max-width: 60ch;
  }
  h1 { font-size: 1.3rem; margin: 0 0 12px; }
  p { font-size: 0.92rem; color: var(--text-sub); margin: 0 0 20px; }
  .back {
    display: inline-block;
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--accent);
    border: 1px solid var(--accent);
    padding: 8px 18px;
    border-radius: 999px;
    text-decoration: none;
  }
  .back:hover { background: var(--accent-soft); }
</style>
